/*
 * Title
 ? Task
 ! Bug
 !! Warning
 + Done(bug or task)
*/

                                        **## BUG ##**
---------------------------------------------
- Remove an item from NoteViewModel.recordList showing remove 2 item but that remove 1 item finally (+)
- If click on two record then two record play together (+)
- When remove playing record sounds still audible (+)
- When remove playing record seekBard lost position (+)
- Go to mark activity and back, you see a lot of record (+)
----------------------------------------------
+  Fix Permissions for different sdks.
+ Remove some checkboxes and don't save that, back to checkbox note and click back, you see CHANGE_DIALOG
+ Fix Storage problem for sdk 29 & above and below
+ Go to mark activity and back, you see a lot of images
!! change nested scroll view location
+ change background in dark mode
+ remove a photo, take another, when you back you see removed image again.
====================================================================================================
                                        **## TASK ##**
/*
    + Enhance image quality (find better scale & nice quality for save and show)
    + Run I/O works on Coroutine
    + Remove voice & image folder when removing note
    + Change item-view on main-fragment -> (don't show mark signs , show bold dot instead # signs
    +                                        for checkbox items)
    + Create custom text-style for sharing checkboxes ex-> ([x] item1 , [] item2)
    ? Create nice Lock-Screen dialog design
    * use corner for image recycler background
    ? Try better style for records
    ? Try better style for sort-dialog
    ? Use same icon theme
    ? Use icon for Application
    ? blue background is similar to blue foreground
    + Implement Voice section and Image section to CheckBox
    + Remove move-icon on checkbox-fragment
    ? Clean codes
    ? Use proguard or something like that
*/



